apiVersion: v1
kind: Service
metadata:
  name: hiclaw-controller
  namespace: hiclaw-system
  labels:
    app.kubernetes.io/name: hiclaw-controller
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hiclaw-controller
  ports:
    - name: http
      port: 8090
      targetPort: http